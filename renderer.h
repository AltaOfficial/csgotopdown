#pragma once
#include <stdio.h>
#include <stdbool.h>
#include "utilities.h"

void display_world(); // takes in map string and prints it to the screen
void game_start(bool* control_friendlys); // not entirely sure what we will use this for